# Benchmark v0 synthetic fixtures

These prediction documents are contract fixtures only. The test builder binds
them to 20 tiny synthetic captures and generates matching public inputs, rights
records, and separately held teacher files in a temporary directory.

Nothing in this directory is a decoder result, a real recording, a measured
benchmark outcome, or evidence of model quality.
